require 'spec_helper'

describe Activeadmin::Custom::Settings do
  it 'has a version number' do
    expect(Activeadmin::Custom::Settings::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
